require('angular');
require('comp-pool-js-client');

var app = angular.module("SemprebetaApp", []);
app.controller("JobsCtrl", ["$scope", "$http", "$interval", function($scope, $http, $interval) {
}]);
